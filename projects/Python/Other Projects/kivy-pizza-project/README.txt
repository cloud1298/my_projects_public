Run main.py

YOU MUST FIRST RUN THE DJANGO SERVER IN A "DJANGO" FOLDER. The program is using this server to fully load data.

This program takes the data from the Django server and then loads and formats the data to show a list of pizzas.